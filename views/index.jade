extends layout

block content
  h1= title
  a(href="/new") Create a new message
  p Welcome to #{title}
  each val in messages
    div.message
      p #{val.user}
      p #{val.text}
      p #{val.added}
